spring:
  application:
    name: license-service
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: refresh

encrypt:
  key: IMSYMMETRIC